# Line Notify System

Line Notify System is a lightweight and modern notification system for FiveM servers, featuring a clean NUI interface and notification history.

The resource is designed to be simple to integrate, performance-friendly, and compatible with all major FiveM frameworks.

---

## ✨ Features
- Modern NUI-based notifications
- Notification history menu (default key: F10)
- Simple export-based API
- Optimized client-side performance
- Framework-agnostic (ESX / QBCore / Qbox / Standalone)

---

## 📦 Requirements
- None


---

## ⚙️ Installation
1. Download the asset 
2. Place the `line_notify` resource into your `resources` folder
3. Add the following line to your `server.cfg`:
